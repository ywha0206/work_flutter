import 'package:flutter/material.dart';

class PostUpdateBody extends StatelessWidget {
  const PostUpdateBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
