// code의 시작점
void main() {
  // data란?
  // 사물이나 현상에서 얻어진 사실 또는 값

  // 변수란?
  // 값을 저장할 수 있는 메모리 공간, 즉 값을 저장해두고 이후에 재사용하거나
  // 변경할 수 있도록 메모리 공간을 이름으로 할당한 개념

  // 데이터타입이란?
  // 메모리 공간에 들어간 값의 형태

  int n1 = 10;
  double d1 = 10.1;
  bool b1 = true;
  String s1 = '홍길동'; //홑따옴표 권장사항

  // dart 기본 메모리 공간 3가지
  // 스택(stack) - 지역변수, 매개변수 LIFO
  // 힙 영역(heap) - new / 리터럴로 선언된 값들 // 객체 같은 것들 동적 영역
  // 정적 영역(Static) - 전역변수, 클래스의 static 변수, 컴파일 시점 상수 등
}
